---
- hosts: localhost
  become: yes
  become_user: root
  tasks:
    - name: Find snapshots.
      ec2_snapshot_facts:
        filters:
        "tag:Name": [QA,Prod,Dev]
      register: ec2_snapshot_facts

    - name: Print snapshot facts
      debug:
        msg: "{{ ec2_snapshot_facts }}"

   
  
  
      
