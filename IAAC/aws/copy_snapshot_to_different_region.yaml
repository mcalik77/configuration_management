---
- hosts: localhost
  become: yes
  become_user: root
  tasks:
    - name: Gathering the facts...
      ec2_snapshot_facts:
        region: us-east-1
        filters:
          "tag:Name": [QA,Prod,Dev]
      register: ec2_snaphot_facts
      
      debug:
        msg: "{{ ec2_snapshot_facts }}"
      
   
  
  
      
